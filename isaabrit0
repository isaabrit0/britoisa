function setup() {
  createCanvas(800, 800);
  background("white");
}

function draw() {
 stroke("black")
 fill("green")
  
  // console.log(mouseIsPressed)
  
  if (mouseIsPressed) { 
   rect(mouseX, mouseY, 100, 100);
  }
}
